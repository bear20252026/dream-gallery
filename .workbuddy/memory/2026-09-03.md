
## 婚礼拱廊 + 喷泉 + 坐标HUD:打包 commit 并完整部署上线 (commit 8b27c7d, a1b467d)

**部署盲区**:Vite 只复制 public/ 进 dist,models/ **不进 dist**——代码用绝对路径 /models/hall/ 加载,但 deploy.sh 上传的是 dist。所以两个新 glb(14.7M)必须**单独 scp 到 /opt/gallery/models/hall/**,否则 deploy 完线上没模型。这条之前没记。

**部署流程(全套)**:
1. `git commit` (代码 + 2 glb + 9 探针 + models-showcase.html + .gitignore)
2. `bash scripts/deploy.sh` (构建 + 上传 dist + 重启 pm2)
3. **单独 scp 两个 glb** 到 /opt/gallery/models/hall/
4. md5 两端比对(必须一致)
5. 探针跑线上:`PROBE_URL=https://cloudbear.cloud PROBE_WAIT_MS=180000 node scripts/probe/verify-fountains.cjs`

**线上探针验收全绿**:
- A 白板残留 0 / B 直径 7.89~7.94m / C 碰撞 1570 盒池心全挡 / D 池底均高于最高地形 / E 20 材质 8 veil + 12 jet uTime +1.22s / errors: none
- 视觉确认:fn-south-view.png 中央 Zsolnay + 三层水 / fn-aerial.png 俯瞰四座对称 / fn-flow-a/b 同机位连拍水在动

**已知旧问题未修**(非本次引入):拱廊 base bbox ≠ 实体中心,三段 posZ=-4.96/9.13/23.22 覆盖 z∈[-18.4,20.0],原馆 z∈[-12,28] → 北端外挑 6.4m、南端缺 8m。俯视图确认南喷泉确实离拱廊远。等用户下次问再修。

**新盲区发现**:models/ 整体在服务器上是散放的(avatar/ character/ hall/ rooms/ remielle/ strawberry_ship/ yazd/),代码按 /models/xxx/ 路径加载。后续换任何模型都得单独 scp。改进方向:vite.config 加 publicDir 同时把 models/ 软链到 public/models/,或 server.js 静态中间件映射 /opt/gallery/models → /models/。**暂未改**(风险 + 收益不匹配,等下次有人栽跟头再改)。
