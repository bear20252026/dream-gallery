# 梦幻画廊 · 后台操作手册(仅限展出方阅读)

> 本文记录后台全部设置项与玩法机关,**不可给访客看**。后台地址:`https://cloudbear.cloud/admin?token=<TOKEN>`(TOKEN 在服务器 pm2 环境里)。

## 一、访客进出

- **开启邀请函即放行**:访客首页看到 💌 邀请函,点「开启邀请函」直接进,无需你同意。身份以 IP+UA 为主、设备指纹(屏幕/时区/语言/平台/canvas)为辅;同一个人换 App(如千问转接)也会归并到同一条记录。
- **昵称**:访客进馆后自己设置(5 秒弹窗 + ⚙设置),你在审批页看到他的最新昵称。
- **审批页仍可**:撤销批准(送回首页)、拒绝、拉黑/关注 IP、备注设备名。

## 二、入馆答题(建筑内第二道门)

- **三套试卷**:理科卷/文科卷(学科题,正解永不下发) + **神话卷**(2026-07-26 新增,题库 `questions/shenhua.json` 独立不混用:90 选择+10 问答,**判后公开正解+解析**,问答题附出题意图+参考方向)。
- 九选择(各 9 分)+ 一问答题(19 分),**≥60 分**(约答对 6 题)门禁墙沉下直接进(2026-07-26 起,原 95)。
- **逐题批改**:选择题点选即判对错(`POST /api/quiz/judge`,文理卷只回布尔、神话卷回正解+解析),**判过锁定不可改**;答对一题,答题背景的金色裂纹亮一丝,满分金光填满现昆仑山巅剪影。
- **可重复答题**(2026-07-26):已解锁也能再进心象共鸣;**天穹进度(100 制)=MIN(答对选择题×1+上传照片×5,100)**,问答写了字即 +1,上传按内容哈希去重防刷;里程碑 11 档自动弹大字+语音。
- **不满 60**:前端展示「特别邀请函」,访客接受后调用 `POST /api/quiz/invite`,同样获得进入权限(与通过等效;即《昆仑灵鉴》文档「镜框回应得慢一些」档)。
- 问答题由 AI 阅卷(双通道:主 moonshot key → 备 Kimi Code key,都在服务器环境/`.env`),评分纪律:切题保底 5、一般 10、优秀鼓励满分;低于 25 字最多 4 分;AI 挂了回退本地细则。
- 权限操作:审批页可「授予 VIP 免答」/「撤销答题权限」,立即生效(SSE 秒级推送)。

## 三、展示区模式(后台「展示区」页)

- **全局默认 = 普通模式**;**「特殊访问」按设备授予**(审批页对应设备点「授特殊访问」),访客自己申请不到。
- 普通模式下:图库照片/视频不上墙(**演示照片**(201~205)与**本人上传**除外)、金山文档(isLink2,4~13)/瑶华传(isLink3)/秘密花园(isGarden)的外链删除(3D 图案保留)、情书卷轴(isLink)改写成《元素共鸣准则》。
- 特殊模式下:现状全展示(演示照片反而不上墙,只后台可见)。
- **自定义链接**:后台「展示区」添加,可挂载到原 3D 图案(火星/木星/地球等,仅普通模式生效),也可新建十种模型;预设快捷:百度/哔哩哔哩/抖音/微博。
- **访客链接**:访客在馆内「📷 上传/链接」自建的链接,此处可查看/复制/删除。
- **演示照片**:「文件管理」里对照片点「设为/取消演示」。

## 四、访客上传与 AI 配文

- 访客「📷 上传/链接」→ 传照片(≤5MB,不限张数)→ 立即上墙,只有自己可见;`/api/vision/analyze` 生成配文(低价视觉模型 `moonshot-v1-8k-vision-preview`,审核不过/失败则用回退句「这张照片,已替你好好珍藏。」)。
- 上墙后金色路标(虚线+流光)引导访客找到作品。
- **你在「文件管理」看到全部照片**(含白板作品):缩略图、AI 配文、上传者、预览/下载/复制链接/删除。

## 五、彩蛋(说明书里只做了暗示,别声张)

- 海拔 **≥90m**:播放 `music/登飞来峰.m4a` 语音一次(降到 80m 以下重新武装)。
- 海拔 **≥100m**:玩家正前方 14m 生成视频屏,播 `videos/1000005388.mp4`,播完自隐(降到 90m 以下重新武装)。
- 彩蛋音频/视频播放期间,户外大屏 5 视频循环自动挂起(`ctx.bigScreenHold`),结束后恢复。

## 六、反刷存储预警(防占盘攻击)

- **四类自动预警**(公开上传后自动检测):🚨 速率(同设备 10 分钟 >8 个文件)、📦 大单(单文件 >300MB)、💾 累计(同设备 >2GB)、🛑 全盘(photos+videos >10GB)。
- **表现**:审批页顶部红色预警卡实时列出(类型/明细/设备/IP/文件),涉事设备自动打「⚠ 可疑」标记(附原因)。
- **处置**:预警卡上「封闭 IP」一键全站拦截;**误封随时可在设备卡片「解除拉黑」恢复**。单条「忽略」/「全部忽略」(后者同时清除可疑标记)。
- 数据存 `gate_data.json` 的 `alerts`(留 500 条),接口 `POST /api/admin/alerts {action:'dismiss'|'clear'}`。

## 七、链接点击档案(仅后台可见,普通用户无任何查看窗口)

- 访客每点一次链接(原 3D 图标/自定义模型/卷轴),都落一条完整档案:时间、昵称、IP+归属地、UA、品牌、电量网络、设备指纹(屏幕/时区/语言/平台/核心/内存/触屏/canvas)、点了哪个链接、目标 URL、馆内坐标。全量保留,存 `gate_data.json` 的 `linkClicks`。
- **答题全量留档**:每次答题的题目、ABCD 四选项内容、访客所选字母、正解字母、对错、问答题目+作答原文+评分评语,都在 `quizAttempts[].fullReview`(访客端不下发,只后台可见)。
- **后台查看**:「展示区」页底部=点击记录;「答题记录」页点开卡片=全量题目详情。
- **导出**:「展示区」页两个按钮——`导出 Excel`(xlsx 两 sheet:链接点击+答题全量,`/api/admin/export.xlsx`);`导出 PDF`(打印页含点击+答题+照片原图附录,浏览器另存为 PDF)。**批量清理**按钮一键清空点击记录。

## 五、聊天室(2026-07-26)

- 设置页「💬 聊天」:全员共享,**只留最近 100 条**,存 `gateData.chat`;发言 ≤140 字、每设备 3 秒一条;昵称=雅号(未起名显示「无名藏梦人」);气泡自己靠右、他人靠左。
- **@昆仑之灵**:发言带 `@昆仑之灵`(或 @机器人)即触发 AI 回帖(lib/chat.js `summonBot`,复用 AI 阅卷双通道密钥,≤60 字古雅口吻,每设备每天 10 次,失败静默)。
- 清理:改 `gate_data.json` 的 `chat` 数组后 `pm2 restart gallery`(运行进程持内存副本,直接改文件会被覆盖)。
- 接口:公开 `POST /api/track/click`(只回 ok,查不到数据);token `POST /api/admin/clicks/clear`、`GET /api/admin/export.xlsx`。

## 八、其他机关

- **房屋换色**:访客「🎨 房屋换色」分组换(墙壁/天花板/腰线/踢脚线),只改自己浏览器(localStorage),不影响别人。
- **媒体加载规则**:答题通过前,室内图片/视频一律不加载(省带宽保大屏);通过后图片放行、视频按距离(近 18m 播/远 22m 停)。
- **视频带宽**:大屏视频全部 ≤500kbps(原码率备份在 `videos/户外大屏/backup_原码率/`),新视频一律先压再传。
- **服务器**:阿里云 `101.133.235.110`,pm2 名 `gallery`,目录 `/opt/gallery/`;SSH 密钥 `~/.ssh/bear_site_key.pem`(与 Downloads 的 bear1.pem 等效)。改 `js/` 静态文件不用重启;改 `server.js`/`lib/` 要 `pm2 restart gallery`。

## 九、测试与部署纪律

- 改完必跑:`node test.js`(80 项)、`node test-mobile.js`(6 项),全绿才部署。
- 部署后跑 `node debug-browser.js https://cloudbear.cloud/ 15` 确认无 pageerror。
- 排障 URL 加 `?shaderdebug` 可恢复着色器同步诊断。
