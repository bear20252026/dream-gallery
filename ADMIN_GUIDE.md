# 梦幻画廊 · 后台操作手册(仅限展出方阅读)

> 本文记录后台全部设置项与玩法机关,**不可给访客看**。2026-09-20 全面校对(对齐 x-token 鉴权/SQLite 主库/CSP 收敛/测试现状)。
>
> **后台入口**:`https://cloudbear.cloud/admin?token=<TOKEN>`(TOKEN 在服务器 pm2 环境里)。
> 页面打开后所有 API 自动改用 `x-token` 请求头(页面自动处理,无需手工)。
> `TOKEN_QUERY=0`(见「环境变量」)时:后台**页面**仍可用 query 打开,但**数据 API 只认 x-token 头**——正常用浏览器后台不受影响。

## 一、访客进出

- **开启邀请函即放行**:访客首页看到 💌 邀请函,点「开启邀请函」直接进,无需你同意。身份以 IP+UA 为主、设备指纹(屏幕/时区/语言/平台/canvas)为辅;同一个人换 App(如千问转接)也会归并到同一条记录。
- **昵称**:访客进馆后自己设置(5 秒弹窗 + ⚙设置),你在审批页看到他的最新昵称。
- **审批页仍可**:撤销批准(送回首页)、拒绝、拉黑/关注 IP、备注设备名。被踢/待重审设备**无法用伪造探针头绕过**门禁(2026-09-18 P1#1)。

## 二、入馆答题(建筑内第二道门)

- **三套试卷**:理科卷/文科卷(学科题,正解永不下发) + **神话卷**(题库 `questions/shenhua.json` 独立不混用:90 选择+10 问答,**判后公开正解+解析**,问答题附出题意图+参考方向)。
- 九选择(各 9 分)+ 一问答题(19 分),**≥60 分**(约答对 6 题)门禁墙沉下直接进。
- **逐题批改**:选择题点选即判对错(`POST /api/quiz/judge`,文理卷只回布尔、神话卷回正解+解析),**判过锁定不可改**;答对一题,答题背景的金色裂纹亮一丝,满分金光填满现昆仑山巅剪影。
- **可重复答题**:已解锁也能再进心象共鸣;**天穹进度(100 制)=MIN(答对选择题×1+上传照片×5,100)**,问答写了字即 +1,上传按内容哈希去重防刷;里程碑 11 档自动弹大字+语音。
- **不满 60**:前端展示「特别邀请函」,访客接受后调用 `POST /api/quiz/invite`,同样获得进入权限(与通过等效)。
- 问答题由 AI 阅卷(双通道:主 moonshot key → 备 Kimi Code key,都在服务器环境/`.env`),评分纪律:切题保底 5、一般 10、优秀鼓励满分;低于 25 字最多 4 分;AI 挂了回退本地细则。
- 权限操作:审批页可「授予 VIP 免答」/「撤销答题权限」,立即生效(SSE 秒级推送)。

## 三、展示区(2026-09-06 起仅普通模式,「特殊访问」已删除)

- **图库照片/视频不上墙**:只有**演示照片**(201~205)与**本人上传**可见;情书卷轴(isLink)改写为《元素共鸣准则》。
- **自定义链接**:后台「展示区」添加,可挂载到原 3D 图案,也可新建十种模型;预设快捷:百度/哔哩哔哩/抖音/微博。
- **访客链接**:访客在馆内「📷 上传/链接」自建的链接,此处可查看/复制/删除。
- **演示照片**:「文件管理」里对照片点「设为/取消演示」。

## 四、访客上传与 AI 配文

- 访客「📷 上传/链接」→ 传照片(≤5MB,不限张数)→ 立即上墙,只有自己可见;`/api/vision/analyze` 生成配文(低价视觉模型,审核不过/失败则用回退句「这张照片,已替你好好珍藏。」)。
- 上墙后金色路标(虚线+流光)引导访客找到作品。
- **你在「文件管理」看到全部照片**(含白板作品):缩略图、AI 配文、上传者、预览/下载/复制链接/删除。

## 五、彩蛋(说明书里只做了暗示,别声张)

- 海拔 **≥90m**:播放 `music/登飞来峰.m4a` 语音一次(降到 80m 以下重新武装)。
- 海拔 **≥100m**:玩家正前方 14m 生成视频屏,播 `videos/1000005388.mp4`,播完自隐(降到 90m 以下重新武装)。
- 彩蛋播放期间,户外大屏 5 视频循环自动挂起(`ctx.bigScreenHold`),结束后恢复。

## 六、反刷防护(2026-09-20 起**超限硬拒**,不再只预警)

- **公开上传(照片/视频/白板)四类自动检测**:🚨 速率(同设备 10 分钟 >8 个文件)、📦 大单(单文件 >300MB)、💾 累计(同设备 >2GB)——**超限即删文件并返回 429**;🛑 全盘(photos+videos >10GB)仍只预警不拦单人(不因他人占盘连坐)。
- **白板作品同样受速率硬拒**(匿名公开通道,2026-09-20 起),且图片过 NSFW 审核(未开启则放行)。
- **表现**:审批页顶部红色预警卡实时列出(类型/明细/设备/IP/文件),涉事设备自动打「⚠ 可疑」标记。
- **处置**:预警卡上「封闭 IP」一键全站拦截;误封随时可在设备卡片「解除拉黑」恢复。单条「忽略」/「全部忽略」。
- 数据存 `alerts`(留 500 条),接口 `POST /api/admin/alerts {action:'dismiss'|'clear'}`。

## 七、链接点击档案(仅后台可见)

- 访客每点一次链接,落一条完整档案:时间、昵称、IP+归属地、UA、品牌、电量网络、设备指纹、点了哪个链接、目标 URL、馆内坐标。存 `linkClicks`(5000 条上限)。
- **答题全量留档**:`quizAttempts[].fullReview`(访客端不下发,只后台可见)。
- **后台查看**:「展示区」页底部=点击记录;「答题记录」页点开卡片=全量详情。
- **导出**:「展示区」页两个按钮——`导出 Excel`(xlsx 双 sheet);`导出 PDF`(浏览器另存)。**批量清理**按钮一键清空点击记录。

## 聊天室

- 设置页「💬 聊天」:全员共享,**只留最近 100 条**;发言 ≤140 字、每设备 3 秒一条;昵称=雅号。
- **@昆仑之灵**:触发 AI 回帖(复用 AI 双通道,≤60 字古雅口吻,每设备每天 10 次,失败静默)。
- 清理:改数据的 `chat` 数组后 `pm2 restart gallery`(进程持内存副本,直接改文件会被覆盖)。

## 其他机关

- **房屋换色**:访客「🎨 房屋换色」分组换,只改自己浏览器,不影响别人。
- **媒体加载规则**:答题通过前,室内图片/视频一律不加载;通过后图片放行、视频按距离(近 18m 播/远 22m 停)。
- **视频带宽**:大屏视频全部 ≤500kbps,新视频一律先压再传。

## 环境变量(服务器 .env / pm2 环境,2026-09-20 校对)

| 变量 | 默认 | 说明 |
|------|------|------|
| `TOKEN` | 空 | 后台与写接口鉴权;**生产必设** |
| `TOKEN_QUERY` | 未设 | 设 `0`:公网禁 query 传 token(仅 x-token 头);后台页面仍可 query 打开,API 用 x-token |
| `CSP_UNSAFE_EVAL` | 未设 | 设 `1` 恢复完整 unsafe-eval(排查 Three 动态着色器;默认 wasm-unsafe-eval 已够用) |
| `CORS_ORIGIN` | cloudbear.cloud | 跨域允许源;本地 localhost 自动放行 `*` |
| `USE_SQLITE` | 开 | 设 `0` 回退 JSON-only(测试探针自动使用) |
| `GATE_DATA_FILE` | 根/gate_data.json | 数据文件重定向(测试隔离用);SQLite db 随之同目录派生 |
| `NSFW_MODERATE` | 关 | 设 `1` 开启上传图片 AI 审核(有调用成本) |

## 服务器与部署纪律

- **服务器**:阿里云 `101.133.235.110`,pm2 名 `gallery`,目录 `/opt/gallery/`;SSH 密钥 `~/Downloads/网站私钥bear1.pem`。
- **改前端**(src/ 或根 html):`npm run build` → 先传 `dist/assets/*` 再传 `dist/*.html`+`sw.js`+`manifest.json`,不用重启。
- **改后端**(server.js/lib/):scp 后**必须** `pm2 restart gallery`。
- **数据层**:SQLite 主库(`gate_data.db`,WAL)+ JSON 镜像原子写双保险;`USE_SQLITE=0` 可回退。
- **上线前必跑**:`npm test`(test-store 4 + test.js 127 断言 + test-mobile 6)、`npx vitest run`(241)、`npm run lint`、`npm run test:scene`(截图回归 4 检查点)、`node scripts/probe/security-fix-probe.js`(13 项,已隔离测试数据不动真实库)。全绿才部署。
- **部署后**:`node scripts/probe/debug-browser.js https://cloudbear.cloud/ 15` 确认无 pageerror。
- **排障**:URL 加 `?shaderdebug` 恢复着色器同步诊断;`?storyreset` 重走 B612 剧情链。
