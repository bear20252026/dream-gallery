# 上线后验证清单 · 昆仑灵鉴·万镜画廊

> 适用前提：已按顺序执行 `git push` → `npm run build` → 上传 `dist` → `pm2 restart`。
> 目标：确认本次 6 项改进（原子写 / 回归测试 / CI / 路由表重构 / CORS 可配 / 清理大文件）在生产环境无回归、可正常运行。
> 用法：每完成一项在 `[ ]` 打勾，异常项记到「遗留问题」区。

---

## 0. 部署前自查（上传前）
- [ ] `npm run build` 本地成功，`dist/` 产物完整（含 `index.html` 与打包后的 JS/CSS）
- [ ] `git status` 干净，已 `commit` 并 `push`（云端与本地同步）
- [ ] 上一版 `dist/` 已备份，便于回滚
- [ ] `videos/` 媒体资源单独打包（该目录被 gitignore，仓库不含，需手动随部署机 media 目录补齐）

---

## 1. 进程与基础健康
- [ ] `pm2 status` 目标进程为 `online`，`uptime` 持续增长，无反复重启（restart 次数不蹭蹭涨）
- [ ] `pm2 logs <app>` 启动无致命错误，重点看：
  - 无 `[store] 保存 gate_data.json 失败`（出现多为数据目录路径权限/不存在）
  - 无 `EADDRINUSE`（端口被占，检查 PORT 冲突）
  - 启动横幅正常打印 API 路由表（20 条路由）
- [ ] 端口监听探活：`curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:<PORT>/` 返回 `200`

---

## 2. 关键环境变量（部署机必须设置，缺一项可能静默异常）
- [ ] `TOKEN`：API 鉴权令牌已设（请求需带 `?token=` 或 `x-token` 头）
- [ ] `CORS_ORIGIN`：生产环境设为具体前端域名，**不要留 `*`**（跨域访问时才需要）
- [ ] `GATE_ANSWER` / `GATE_QUESTION` / `GATE_MODE`：答题门/审批门配置已按预期设置
- [ ] `MEDIA_DIRS`：媒体目录已指向部署机真实路径
- [ ] `GATE_DATA_FILE`（可选）：如自定义数据文件路径，需指向**持久且可写**目录（默认 `ROOT/gate_data.json`）

> 验证环境变量是否生效：在 `pm2 logs` 中确认对应配置被读取，或用带 token 的请求实际触发一次保存后检查落盘路径。

---

## 3. API 回归（路由表重构后重点验）
> 路由已从 if/else 重构为 routes 表，语义应 100% 一致。逐项核对：

- [ ] `GET /api/files?dir=photos` 返回文件列表（`photos/` 已入库，应有内容）
- [ ] `GET /api/files?dir=videos` 返回列表（仓库不含 videos，需确认部署机 media/videos 已补齐，否则为空属正常）
- [ ] `GET /api/files?dir=music` 返回列表
- [ ] `POST /api/upload?dir=photos&name=test.jpg` 带 token 上传一张测试图 → 返回成功且磁盘文件存在
- [ ] `DELETE /api/files/photos/test.jpg` 带 token 删除 → 返回成功且磁盘文件已移除
- [ ] 答题门/审批门接口（按 `GATE_MODE`）读写 `gate_data.json` 正常
- [ ] 不带 token 访问写接口应被拒（401/403），确认 TOKEN 门禁生效

---

## 4. 数据持久化（原子写 & 写锁验证）
- [ ] 连续触发多次保存（如连点答题/审批），检查 `gate_data.json` 无截断、内容可 `JSON.parse`
- [ ] 正式文件旁**无残留 `.tmp`**（`saveGateData` 用临时文件 + rename 覆盖，正常完成后 tmp 应消失）
- [ ] 崩溃恢复模拟：手动把 `gate_data.json` 末尾截断一行（破坏 JSON），`pm2 restart` → 确认 `loadGateData` 自动恢复为合法 JSON，服务可正常启动

---

## 5. 前端 3D 画廊
- [ ] 浏览器打开站点，Three.js 画廊加载，控制台**无报错**（WebGL 上下文、资源 404 等）
- [ ] `photos/` 媒体正常显示；依赖 `videos/` 的模块确认部署机已补齐资源
- [ ] 自适应画质 / 移动端布局正常（历史亮点，本次未改但需确认无回归）
- [ ] 上传、删除、答题门/审批门等交互端到端跑通

---

## 6. 安全核查
- [ ] 上传接口必须带 token，匿名上传被拒
- [ ] 删除接口必须带 token
- [ ] 生产 `CORS_ORIGIN` 不为 `*`
- [ ] 身份归属用不可猜测 `vid` Cookie（A01 反伪造）仍生效

---

## 7. 日志与监控（上线后观察 24h）
- [ ] `pm2 logs` 错误率为 0 或仅预期内（如媒体缺失警告）
- [ ] 磁盘：`gate_data.json` 与 media 目录增长符合预期，无异常膨胀
- [ ] 进程内存/CPU 平稳，无内存泄漏迹象

---

## 8. 回滚预案
- [ ] 保留上一版 `dist/` 备份路径已记录
- [ ] 回滚方式明确：
  - 快速：`pm2 stop <app>` → 换回旧 `dist` → `pm2 restart <app>`
  - 或 `git checkout <上一tag/commit>` → 重新 `npm run build` → 上传 → `pm2 restart`
- [ ] 数据回滚：`gate_data.json` 有备份（JSON 文件库，备份即复制文件）

---

## 遗留问题记录
| 项 | 现象 | 影响 | 负责人 | 状态 |
|----|------|------|--------|------|
|    |      |      |        |      |

---

## 已知环境坑（非代码 bug，验收时勿误判）
- **干净克隆跑不了完整 `test.js`**：`videos/` 被 gitignore，集成测试依赖媒体资源，仅在部署机/含媒体环境跑。
- **沙箱内 `fs.unlink` 被劫持**到回收站通道，导致 DELETE 类集成测试在沙箱失败；真实 Linux 正常。验收以部署机实际表现为准。
- **Git-Bash 路径转换**：本地调试用 `GATE_DATA_FILE` 时路径可能被转成 `D:\d\...` 幻影路径，属 shell 环境问题，非 `store.js` bug。
