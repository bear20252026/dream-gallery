# 快赢批 C8 / B3 / D4 / B6 / C2 — 上线总结

> 日期：2026-07-30　|　部署目标：`101.133.235.110` → `cloudbear.cloud`（pm2 应用 `gallery`）

## 一、本批交付的 5 个能力
| 任务 | 能力 | 关键落点 |
|------|------|----------|
| **C8** NSFW 审核 | 上传加 vision 双通道判定，违规图拒传并提示「这片灵蕴暂时无法归位」 | `lib/files.js`：`moderateImage` + `modImgB64`（ffmpeg 512px 降采样）；`NSFW_MODERATE=1` 时启用，失败兜底放行 |
| **B3** 飞舟结界 | 六灵蕴未齐时泊位升起半透明结界软推玩家，六齐后消融 | `src/kunlun/ark.js`：`barActive` / `barTick` 状态机 + 软推（水平距<8.5 外推+modeToast） |
| **D4** 低画质开关 | 设置里加「流畅 / 自动」手动开关 | `src/main.js` `setLowQuality` + `src/gate/settings.js` `qOv` 浮层 + `store.js` `lowQuality` 字段 |
| **B6** TTS 音色分层 | 收集/航路/展厅/称号 分音色（Xiaoxiao/Yunxi/Xiaoyi/Yunyang） | `src/scene/media.js` `KUNLUN_VOICES` 转发；`lib/tts.js` 音色键；`lib/aichannels.js` `synthTts(text, voice)`；4 处调用点打标 |
| **C2** 展厅选片导入 | 永恒展厅从本人上传选 ≤20 幅，西墙呈现，可见性走 mediarules | `server.js` `/api/myuploads`；`lib/files.js` `handleMyUploads`；`src/kunlun/eternal.js` 西墙网格；`src/gate/settings.js` 选片器 |

## 二、质量门禁（部署前必过）
- `scripts/test/test.js` → **127 通过 / 0 失败**
- `scripts/test/test-mobile.js` → **6 通过 / 0 失败**（iPhone 12 模拟：canvas / shader / JS异常 / 空屏 / 灯光限额 全过）

## 三、构建与部署
- `npm run build` → `dist/`（vite，✓ built in 4.53s）
- 前端：SOP `rm -rf assets && tar xzf`（标准整目录上传）
- 后端：因改了 `server.js` + `lib/`，**额外 scp 4 个后端文件**到 `/opt/gallery`（server.js / lib/tts.js / lib/files.js / lib/aichannels.js）；**不动** `gate_data.json` / `.env` / `photos` / `videos` / `origin`（TLS 私钥）
- `pm2 restart gallery` → 新 pid 161871，`online`

## 四、公网验证
- `node scripts/probe/debug-browser.js https://cloudbear.cloud/ 15`
- 结果：**零 PAGE_ERROR / 零 FATAL / 零服务端 4xx/5xx**
- 唯一 `REQ_FAIL`：落地页 CDN 视频 `幻夜大屏1号.mp4` 的 `net::ERR_ABORTED`（浏览器端媒体预载中止，旧有，与本批无关）→ 不计入失败

## 五、版本与待办
- 本地提交：`fd19937`（13 文件，+294/-34）
- **GitHub push 受阻（环境限制，非代码问题）**：
  - HTTPS 到 `github.com` 连接被 reset；
  - SSH 22 可达，但本机 `~/.ssh/id_ed25519` 未被 `bear20252026/dream-gallery` 账户授权（Permission denied）
  - → 需用户在 GitHub 可达环境执行 push，或把 `id_ed25519.pub` 加入账户后走 SSH 推
- 可选：若 `cloudbear.cloud` 走 Cloudflare，建议手动清一次根缓存（index.html 已更新），以防边缘返回旧块
