# 代码审查意见 · 梦幻画廊（昆仑灵鉴·万镜画廊）

> 审查人：资深开发工程师 ｜ 审查对象：本地工作副本 `D:\b - 副本 (3) - 副本`
> 审查范围：后端 `server.js` + `lib/`（15 模块）、前端 `src/`（33 模块）、构建/测试/文档
> 版本基准：`git log` 最新 `ce4298c`（分片上传，2026-07-28）

---

## 一、完整性结论：✅ 完整，可上线

| 维度 | 结果 |
|------|------|
| 前端 import 落地 | 33 个 `main.js` import 全部存在（gallery/gate/kunlun/scene/state/ui/shared/styles 齐全）|
| 后端模块引用 | `server.js` 引用的 14 个 lib + `aichannels.js`（被 chat/quiz/tts/vision 复用）全部存在，**无孤儿模块** |
| 桩代码 / TODO | 扫描 `TODO/FIXME/stub/未实现/占位` 命中均为占位纹理/UI 占位文本，**无未完成功能桩** |
| vendor 依赖 | `three.module.js`、`hls.mjs` 本地已就位（gitignored，postinstall 同步）|
| 构建产物 | `dist/` 已构建（gitignored，部署前 `npm run build`）|
| 测试 | `scripts/test/test.js`（后端 127 项）+ `scripts/test/test-mobile.js`（手机端 6 项）齐全 |

**亮点（已是团队级好实践，建议固化成规范）：**
- `main.js` 的 ticker 容错隔离（单 ticker 连续报错 60 帧自动移除）、自适应 pixelRatio 画质调节、灯光限额预算——性能治理到位。
- `files.js` 的上传安全：分片 + 流式中途超限即断流、`isValidName`/`safeJoin` 防穿越、防同名覆盖、SVG 加 CSP、SVG/html 全程 `nosniff`、admin 页 `X-Frame-Options`——OWASP 级。
- `store.js` 的身份归属从 sha1(UA) 升级到不可猜测 `vid` Cookie（A01 反伪造），文档里明确标注了"依据"。

---

## 二、必须修的硬伤（按优先级）

### 🔴 P0 · `gate_data.json` 写入非原子（数据丢失风险）
`lib/store.js:27-29`：
```js
function saveGateData() {
  fs.writeFileSync(DATA_FILE, JSON.stringify(gateData, null, 1));
}
```
**问题**：直接 `writeFileSync` 覆盖。进程在写入中途崩溃（或被 pm2 重启）→ 文件截断 → JSON 损坏 → 访客库/审批/上传归属**整库丢失**。`saveGateData()` 调用极频繁（每次访问、每次上传都会写）。
**修复（临时文件 + rename，同盘原子）**：
```js
const tmp = DATA_FILE + '.tmp';
fs.writeFileSync(tmp, JSON.stringify(gateData, null, 1));
fs.renameSync(tmp, DATA_FILE);   // rename 在同文件系统上是原子的
```
**建议作为一次"教学型修复"**：让团队里 junior 照此改 + 写一条回归测试，正好练手。

### 🟠 P1 · JSON 文件当数据库，无法扛多 worker / 多实例
`gate_data.json` 在进程启动时整体读入内存，靠单例对象共享。一旦 `pm2` 起 **cluster 多 worker** 或做**水平扩容**，各进程内存副本互相覆盖，写丢失。
**现状**（单实例 pm2 `gallery`）可忍；但规模上来前应有预案：
- 短期：给 `saveGateData` 加一个 `AsyncLock`（单进程内串行写）。
- 中期：把访客/审批数据迁到 **SQLite**（零额外服务、文件即库、`better-sqlite3` 同步 API，改动面小）。

### 🟠 P1 · 工作目录混入审计工具遗留大文件（非代码，但污染最新副本）
这些文件**已被 gitignore**（不会进仓库），但堆在"本地最新副本"里：
- `.audit-tmp-repo.bundle`（**40 MB** git bundle）
- `.audit-tmp-100k/500k/1m.jpg`、`.audit-tmp-tiny.png`、`.audit-tmp-up2.png`、`.audit-tmp-upflow.js`
- `.docs-bak/` + `梦幻.zip`（**776 MB**）
**建议**：直接删除（git 不跟踪，删了无妨），或加一条清理脚本。它们会让"本地最新副本"体积虚高、容易误传误判。

---

## 三、质量提升建议（团队能力拉升点）

### 🟡 P2 · CI 缺失，"全绿才部署"靠人盯
README 自己写了"上线前必跑，全绿才部署"，但仓库**无 `.github/workflows`**。建议加最小 GitHub Actions：push 时跑 `node scripts/test/test.js` + `test-mobile.js`（后者需 playwright-core，已在 devDeps）。让"全绿"变成**强制门禁**而非口头规矩。

### 🟡 P2 · `server.js` 路由是单条巨型 if/else 链
200+ 行的 `handler` 里路由全靠顺序 `if`。功能少时没问题，团队变大后可读性差、易漏。建议抽成一张路由表：
```js
const routes = [
  ['GET','/api/files', handleList],
  ['POST','/api/upload', handleUpload],
  // ...
];
```
（这是重构练手的好素材，但**不在本期必须**，注意别为了重构引入回归。）

### 🟡 P2 · CORS `Access-Control-Allow-Origin: *`（低风险）
`server.js:75` 对所有响应放行 `*`。`/api/upload` 公开本就设计如此，且 token 接口仍校验；风险低，但后台/管理类接口可收严到指定源。

### 🟢 P3 · 小瑕疵
- `store.js:30-31`：`if (GATE_MODE==='approval') loadGateData(); else loadGateData();` 两个分支调同一函数，可合并。
- README「lib/ 14 模块」清单未包含 `aichannels.js`（现已被 4 个模块复用）——文档轻微滞后，补一行即可。
- `dist/` 与 `vendor/` 是 gitignored，部署流程务必保证"先 `build` 再 rsync"，否则线上跑的是旧包。

---

## 四、关于"云端版本和本地版本不同步"

当前 git 状态：
```
On branch main
Your branch is ahead of 'origin/main' by 3 commits.   ← 本地领先 GitHub 3 个提交
nothing to commit, working tree clean                 ← 本地工作区干净
```
**结论**：本地确实是更新的一方（你说的"以本地为最新"成立）。但"云端"（GitHub 公开仓(⚠️ PUBLIC) / 阿里云源站 / Cloudflare 边缘）未必是这 3 个提交。

**建议的同步动作（照 SOP 顺序）**：
1. `git push origin main` —— 先把本地 3 个提交推到 GitHub（代码异地备份）。
2. `npm run build` —— 重建 `dist/`（gitignored，必须本地生成）。
3. 按 `SOP-代码结构与改动部署.md` 把 `dist/` 全量上传 `/opt/gallery`，再 `pm2 restart gallery`（仅改 `server.js`/`lib/` 时才需重启）。
4. 确认 `.env` / `origin/` 证书已就位（均 gitignored，靠云端环境或手动放置）。

---

## 五、对"提升团队技术水平"的实操建议
1. **把现有好实践写成团队标准**：ticker 容错、性能预算、上传安全三件套、身份归属反伪造——这些已体现在代码+注释里，抽成一份《前端 Three.js 项目规范》+《Node 零依赖后端安全基线》。
2. **P0 原子写 + P1 单进程写锁** 作为 junior 的第一次 PR 练习，配回归测试，你做 code review。
3. **加 CI 门禁**，把"全绿才部署"从人治变自动化。
4. **部署 checklist 固化**到 PR 模板 / 发布流程，杜绝"云端还是旧版"反复发生。

---
*审查基于静态读取核心链路；未运行服务与测试（需 node 环境 + ffmpeg + playwright-core）。如需我直接动手修 P0 原子写 + 加一条回归测试，说一声即可。*
