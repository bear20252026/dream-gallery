# 梦幻画廊（dream-gallery）全面代码审计报告

> 审计日期：2026-09-18 · 版本基准：`ae27a88`（main，与 origin 同步）  
> 审计范围：全仓库（src/ 前端 148 文件 · 27,444 行；lib/ 后端 25 文件 · 3,675 行；server.js 205 行；scripts/ 测试与探针 196 文件 · 16,080 行；CI/工程化配置；历史审计遗留项复核）  
> 方法：静态源码通读 + 关键路径抽查 + 历史问题逐条对照 + **本机实际执行测试门禁**（见 §7）

---

## 一、项目概况与规模

| 维度 | 现状 |
|------|------|
| 定位 | 自托管 3D 交互画廊（Three.js 漫游 + 答题门禁 + 审批/踢出 + 上传策展 + 昆仑剧情线） |
| 技术栈 | 原生 ESM + Three.js 0.160 + Vite 8 + Vitest；后端 Node http + better-sqlite3 + ws |
| 前端规模 | **src 27,444 行 / 148 文件**（较 2026-09-01 审计约 2 万行增长约 35%） |
| 后端规模 | lib 3,675 行 / 25 模块 + server 入口 205 行（路由已拆 `lib/routes.js`） |
| 自研合计 | **约 3.1 万行核心代码**（不含 vendor ~8 万行、scripts 探针） |
| 测试资产 | Vitest **20 文件 / 228 断言（实跑全过）**；后端 test-store+test.js+test-mobile；探针 **107 个** |
| CI | `.github/workflows/test.yml`：lint → typecheck → unit → build → 后端回归 → 冒烟 → 场景截图回归；bundle 512KB 硬门禁 |
| 数据层 | SQLite 主持久层（`gate_data.db`，WAL）+ JSON 镜像原子写兜底；`USE_SQLITE=0` 可回退 |
| 运维 | 阿里云 pm2 + Cloudflare/R2 + GitHub **公开仓**（密钥纪律见 .gitignore 注释） |

**结构分层（健康）**：`core/`（loop/组合根/game-state）· `scene/` · `gallery/` · `gate/` · `kunlun/` · `shared/` · `ui/overlay` · `state/store` · `lib/` 后端声明式路由。

---

## 二、总体评价

**结论：工程素养明显高于典型个人项目；历史高危项已清零；综合质量约 8.5 / 10（较 2026-09-01 的 8/10 上调）。**

| 维度 | 评分 | 说明 |
|------|------|------|
| 功能完整性 | ★★★★★ | 主世界/B612/六星球/剧情链/门禁/后台/官网一体化 |
| 架构清晰度 | ★★★★☆ | 分层+组合根+声明式路由到位；ctx 扁平写与大文件仍在 |
| 安全基线 | ★★★★☆ | 历史 P0 全修；遗留 token-query、CSP、Cookie Secure、探针绕过门禁 |
| 工程质量 | ★★★★★ | CI 全链路、107 探针、lint-staged；checkJs 债已超警戒线 |
| 性能 | ★★★★☆ | 小地图预渲染/灯光预算/自适应画质已落地；大文件内仍有热点 |
| 文档 | ★★★★☆ | AGENTS/ADMIN_GUIDE/RFC 齐全；README 部分表述滞后 |
| 测试实跑 | ★★★★☆ | lint/typecheck/vitest228/后端97/手机6 **全绿**；安全探针 2 假红 |

**适合场景**：私享运营 + 代码资产参考/转让；不宜直接作为多租户 SaaS 底座。

---

## 三、历史问题复核（是否仍开放）

| 来源 | 历史问题 | 状态 | 证据 |
|------|----------|------|------|
| AUDIT-代码审计 P0 | SVG 上传 XSS | **已修** | `lib/files.js:154` 白名单无 `.svg`；存量 SVG CSP `script-src 'none'`（:68） |
| AUDIT-代码审计 P0 | 分片全量读入 OOM | **已修** | `files.js:307+` 流式拼接；单片 400KB 上限 |
| AUDIT-代码审计 P0 | `e.message` → innerHTML | **已修** | `quizgate.js:402,520` 均经 `escH()` |
| 资深意见 P0 | gate_data 非原子写 | **已修** | `store.js` `.tmp`+`rename`；SQLite WAL 主写 |
| 资深意见 P1 | JSON 多实例不可扩展 | **已修** | SQLite 主持久层（2026-09-01） |
| 资深意见 P2 | 无 CI | **已修** | `test.yml` 双 job 全绿门禁 |
| 资深意见 P2 | server.js 巨型 if 链 | **已修** | `lib/routes.js` 声明式表 + `lib-routes.test.js` |
| 2026-09-01 建议① | 路由拆分 / token 会话化 | **部分** | 路由✅；token 仍支持 `?token=` query |
| 2026-09-01 建议② | 收敛 CSP | **未做** | `server.js:97` 仍 `unsafe-inline`+`unsafe-eval` |
| 2026-09-01 建议③ | 大文件补测拆分 | **部分** | 逻辑拆出 planet/light/film；scene/ark 等仍大 |
| 2026-09-01 建议④ | 渐进 TS | **部分/债涨** | checkJs 现 **110** 错（曾记基线 87）；CI 防新增但仍警告级 |
| 2026-09-01 建议⑤ | 上传限速配额 | **部分** | 有 abuse **预警**不硬拦；vision/tts/chat/wish 有限流 |
| OWASP 2026-07-28 | 2🔴+5🟡 | **已修** | timingSafeEqual、linkClicks 5000、vision 20/日、admin `j()`、安全头等 |
| 2026-08-31 审计 | H1/H2/H4/M1/M8 | **已修** | uncaught 兜底、白板扩展名 guard、非 public 上传上限、IP 拉黑前置、反斜杠 404 |

---

## 四、问题清单（本次发现）与修复状态

> **2026-09-18 当日「全部修改」落地情况**：P1 安全五项 + 主要 P2/P3 可修项已改；下列标注 **已修** / **部分** / **遗留**。

### P0 — 未发现需立即下线的漏洞

历史 P0 均已关闭。

### P1 高优先级

| # | 问题 | 状态 | 修复说明 |
|---|------|------|----------|
| 1 | 自动化 UA / `x-probe:1` 绕过入口门禁 | **已修** | `entryGate` 先判定 kicked/reapply，再才允许 `isAutomated` 跳过建档（`lib/gate.js`） |
| 2 | 管理 token 仍接受 query 串 | **已修（兼容）** | `admin.html` API 全改 `x-token` 头；`tokenOk` 优先 header；`TOKEN_QUERY=0` 时公网禁 query |
| 3 | Cookie 缺少 `Secure` | **已修** | `cookieAttrs(req)`：HTTPS / cloudbear.cloud 自动加 `Secure`（`lib/gate.js`） |
| 4 | JSON 响应 CORS 恒 `*` | **已修** | `sendJson` 不覆盖 handler 已设 ACAO；未设时用 `CORS_ORIGIN`（默认 cloudbear.cloud） |
| 5 | CSP `unsafe-eval` | **部分** | 默认改为 CSP3 `wasm-unsafe-eval`（Three/WebAssembly 可用）；`unsafe-inline` 仍保留（多页内联脚本）；`CSP_UNSAFE_EVAL=1` 可回退 |

### P2 中优先级

| # | 问题 | 状态 | 修复说明 |
|---|------|------|----------|
| 6 | 未配置 TOKEN 时公网 write 放行 | **已修** | 非 localhost Host 的 `auth:'write'` 一律 401（`lib/routes.js`） |
| 7 | OPTIONS 默认 CORS `*` | **已修** | 与 `applySecurityHeaders` 同轨：本地 `*`，生产 `CORS_ORIGIN`（默认 cloudbear.cloud） |
| 8 | 公开上传只预警不阻断 | **已修** | `abuseCheck` 返回 block；rate/bigfile/volume 超限删文件并 429；storage 仍预警不拦单人 |
| 9 | NSFW 默认关闭 | **遗留** | 成本策略，保持 `NSFW_MODERATE=1` 可开 |
| 10 | 非 HMR 模块监听器无清理 | **遗留** | 全页生命周期、改动面大，建议独立 PR |
| 11 | localStorage 绕过 store | **已修** | `audio-manager` 已用 `ctx.store.num('welcomed')` + SCHEMA `welcomed`；`visitor-fp` 为门禁前采集器，文档豁免 |
| 12 | gameState.set 单写未收完 | **遗留** | 架构长线，读路径已稳 |
| 13 | 大文件仍多 | **遗留** | `files.js` 已拆 static/upload；scene/ark 等另 PR |
| 14 | geoLookup http | **已修** | 改为 `https://ip-api.com` |
| 15 | 业务大文件单测薄 | **遗留** | 依赖 test.js 全链路已绿 |
| 16 | console.log 残留 | **遗留** | 多为剧情/婚礼诊断日志，影响低 |
| 17 | 文档/注释滞后 | **部分** | README/`.env.example`/`files-upload` svg 注释已更新 |
| 18 | recordVisit 重复判断 | **已修** | 当前代码已无重复 `isAutomated` |
| 19 | depcruise 无规则 | **已修** | 新增 `.dependency-cruiser.cjs`（循环/gate→ark 等） |
| 20 | checkJs 110>90 | **遗留** | 类型债需分批压（ctx/scene-manager） |
| 22 | security-probe × SQLite | **已修** | 探针 `USE_SQLITE=0` + 播种前清 db；**13/13 全绿** |

---

## 五、安全控制现状（正面确认）

1. **鉴权**：`tokenOk` 长度相等 + `timingSafeEqual`；admin 路由统一 `auth:'token'` fail-closed。  
2. **静态黑名单**：隐藏目录/`lib`/`scripts`/`questions`/`gate_data`/根级 json、非白名单 js；`src|vendor` 仅 localhost。  
3. **路径**：`safeJoin` 带 `path.sep`；`isValidName` 拒斜杠；`decodeURIComponent` try/catch；反斜杠路径 404。  
4. **上传**：扩展名白名单（无 SVG）、50/700MB 流式限流、公开禁覆盖、白板 `whiteboard-*.图片` guard、分片 24h 清扫。  
5. **媒体门禁**：`mediarules.mjs` 单一源；门禁媒体 `private, no-store`；`mt` 令牌。  
6. **限流**：TTS 30/日 · vision 20/日 · 聊天 3s · 念墙 3/日 · bot 10/日 · client-errors 30/分 · linkClicks 5000 上限。  
7. **会话**：`vid`/`pass` HttpOnly + SameSite=Lax + 签名；可吊销 `revokeSession`。  
8. **答题**：`correctLetter` **不下发**（`quiz.js:67,96-97` 仅洗后 options）；仅神话卷判后特批公开。  
9. **仓库**：无私钥/AK/token 明文；`.env`/`gate_data.*`/`*.pem` gitignore；公开仓警示写进 ignore 注释。  
10. **进程**：`uncaughtException`/`unhandledRejection` 兜底不崩进程。

---

## 六、架构与工程质量

### 6.1 做得好的
- **声明式路由表** + 契约测试，杜绝漏挂鉴权。  
- **组合根 / gameState / overlay / store** 四件套深模块化，方向清晰。  
- **三层测试**：纯逻辑单测 + 路径探针 + 截图回归；CI 强制。  
- **性能资产**：灯光预算、小地图纸图预渲染、自适应 pixelRatio、shader 预编译、分片上传抗 CF 524。  
- **血泪文档化**：AGENTS.md 将约束写成可执行规矩（弹层/存档/ctx/媒体/铁山等）。

### 6.2 主要拖累
- **ctx 双轨写** 与 **16 个巨型文件** 是可维护性两大成本中心。  
- **业务 UI 模块**（paintings/links/quizgate）innerHTML 密集（全 src ~51 处赋值），依赖调用点自觉 `escH`——建议 lint 规则或模板封装。  
- **无账号体系**：单静态 TOKEN + query，适合主人私用，公开多管理员场景不够。

---

## 七、测试与 CI 摘要（2026-09-18 修复后复跑）

| 层级 | 命令 | 结果 |
|------|------|------|
| ESLint | `npm run lint` | **EXIT 0 全绿** |
| typecheck | `tsc --noEmit` | **EXIT 0 全绿** |
| 单元 | `npm run test:unit` | **24 文件 / 241 断言全过** |
| 后端原子写 | `test-store.js` | **4 / 4 通过** |
| 后端 API | `test.js` | **97 通过 / 0 失败** |
| 手机端 | `test-mobile.js` | **6 / 6 通过**（CSP 已用 `wasm-unsafe-eval`，无 WASM/JS 异常） |
| 依赖方向 | `npm run deps` | **无违规**（已接 `.dependency-cruiser.cjs`） |
| 安全探针 | `security-fix-probe.js` | **13 / 13 全绿**（SQLite 播种修复后） |
| npm audit | npmmirror | 端点仍不可用（环境限制） |

**修复要点**：
- 门禁：kicked 优先于 `isAutomated`，堵住 `x-probe`/空 UA 绕过。
- Cookie：`cookieAttrs` 按 HTTPS/生产域追加 `Secure`。
- token：后台 API 走 `x-token`；`TOKEN_QUERY=0` 可关公网 query。
- CORS：`sendJson`/OPTIONS 与生产域名策略对齐，不再恒 `*`。
- CSP：默认 `wasm-unsafe-eval`（替代完整 `unsafe-eval`）；`unsafe-inline` 仍因多页内联保留。
- 上传反刷：rate/bigfile/volume 硬 429；探针 `USE_SQLITE=0`。

**仍遗留**（不阻塞本批）：NSFW 默认关、监听器 teardown、gameState 全量收口、scene/ark 大文件拆分、checkJs 债 110。

---

## 八、价值与工作量估算（更新）

| 维度 | 评估 |
|------|------|
| 自研规模 | **约 3.1 万行**核心 + 1.6 万行测试探针（2026-09-01 约 1.6–1.7 万核心，**+70%**） |
| 工程参考价值 | **高**：分层、CI、探针、安全基线、运维文档可作个人/小团队范本 |
| 产品价值 | **中**：私享 3D 画廊玩法独特；通用性有限 |
| 可复用价值 | **中高**：core/（loop、game-state、EntityRegistry、SpatialIndex、light-budget、mediarules）可抽包 |
| 重做成本（外包口径） | **约 30–40 人日×… → 200–260 人日 ≈ 8–13 人月**；中性报价 **30–45 万元** |
| 二手资产/转让 | **6–12 万元**；含部署文档与 1–3 月维护可至 **12–18 万元** |

> 相对 2026-09-01（25–35 万重做 / 5–10 万转让）上调，主因：代码量增长、SQLite+CI+路由拆分+剧情/星球/小地图羊皮纸等新系统、探针库翻倍。

---

## 九、改进建议（按优先级）

1. **堵门禁绕过（P1#1）**：限制 `isAutomated`/`x-probe` 豁免面，确保 kicked 设备无法靠空 UA 进馆。  
2. **token 出 query（P1#2）+ Cookie Secure（P1#3）+ sendJson CORS 统一（P1#4）**——三小改，收益高。  
3. **修 security-fix-probe × SQLite 播种**（否则安全探针永久假红）；checkJs 债从 110 压回 ≤90（优先 `ctx.js`/`scene-manager`）。  
4. **生产无 TOKEN 拒绝公网 write**；abuse 超限改硬 429（可配豁免）。  
5. **收敛 CSP**（去 `unsafe-eval` 评估 Three；内联 nonce）。  
6. **写路径收口**：新代码强制 `gameState.set`；扁平写 lint 告警；`kunlunWelcomed` 入 store。  
7. **拆 3 个最大文件**（scene/openfilm/ark）并补上传/门禁黄金路径单测。  
8. **文档对齐**：README 去掉特殊模式/JSON-only/14 模块/「127 项」等过时句；`files.js` 注释去 svg。  
9. **补 dependency-cruiser 配置**，把 `npm run deps` 从「无规则」变成真门禁。

---

## 十、审计边界与免责

- 基于 **本地工作副本**（Windows + Node v24.18.0）静态审计 + 本机门禁实跑；**未对生产 `cloudbear.cloud` 做动态渗透**。  
- **已实跑**：lint / typecheck / vitest / test-store / test.js / test-mobile / deps / security-fix-probe（结果见 §7）。  
- **未跑**：`test:scene` 截图回归、`smoke-gate` 冒烟、overlay/store/media-rules 等其余 ~100 探针（需 Chromium/媒体，CI 已覆盖配置）。  
- `npm audit` 因 npmmirror 未实现 advisory 端点而失败；依赖 CVE 建议在可访问 registry.npmjs.org 的环境重跑。  
- 价值估算为行业口径参考，非交易定价意见。

---

*本报告由 2026-09-18 会话审计生成；取代/叠加于 `AUDIT-代码审计报告.md`（2026-07-31）与 `审计-整体评价与价值工作量估算-2026-09-01.md`。*
