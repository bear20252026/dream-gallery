# 梦幻画廊（dream-gallery）代码审计报告

> 审计日期：2026-09-01 · 审计范围：全仓库（src/ 前端约 2 万行、lib/ 后端、server.js、lib 各模块、测试与工程化配置）

---

## 一、项目概况

- **定位**：个人作品/纪念向的"3D 画廊"Web 应用——Three.js 第一人称漫游展厅（挂画、视频墙、音乐演奏、沙海/昆仑等场景），配套 Node.js 零依赖后端（静态托管 + 上传/删除 + 访客审批门 + 聊天 + 统计 + TTS + AI 配文等）。
- **规模**：src/ 前端约 20,000 行（~100 文件），后端 lib/ 约 2,400 行 + server.js 路由 332 行；另有 vendor/（Three.js 官方 examples 副本，非自研）。
- **技术栈**：原生 JS（ESM，无框架）+ Three.js 0.160 + Vite 构建 + Vitest 单测；后端纯 Node http + better-sqlite3/ws/AWS S3 SDK；工程化含 ESLint/Prettier/Husky/dependency-cruiser/CI（GitHub Actions）。
- **历程证据**：代码注释与多份审计/上线文档（2026-08-22 至 08-31）显示项目经过多轮"审计→修复→上线验证"迭代。

## 二、整体评价

**结论：这是一个工程素养明显高于典型个人项目的作品，质量分约 8/10。**

### 优点
1. **架构清晰**：前端按 engine/scene/gallery/kunlun/gate/shared 分层，有 ctx 共享上下文总线、事件总线、EntityRegistry、SpatialIndex、worker-pool、资源管理器等基础设施抽象，模块边界明确（并用 dependency-cruiser 守护依赖方向）。
2. **安全意识好**：后端有 CSP/CORS/安全头、静态文件黑名单（隐藏文件、.bak/.sh/.pem、lib/src/scripts 公网 404）、safeJoin 防路径穿越、反斜杠绕过防护、token 鉴权、上传白名单与 NSFW 处理；历史审计问题（如 client_errors.json 公网可下载）有记录并已修复。
3. **工程质量高**：11 个 Vitest 单测文件覆盖物理、碰撞、状态机、空间索引、游戏循环等核心逻辑；CI、lint-staged、husky、typecheck（checkJs）、smoke/probe 脚本齐全。
4. **健壮性**：uncaughtException/unhandledRejection 兜底、畸形 URL 防崩、SSE 推送、客户端错误上报闭环、缓存强刷机制。
5. **文档完善**：AGENTS.md、ADMIN_GUIDE.md、上线验证清单、多份审计报告，可维护性好。

### 主要不足 / 风险
1. **server.js 巨型路由表**：所有路由集中在入口 if/路由表，随功能膨胀（已 30+ 端点），建议拆分模块化路由。
2. **CSP 含 `unsafe-inline` + `unsafe-eval`**：削弱了 XSS 防护价值（有内联脚本与 Three.js 的现实约束，但可逐步收敛）。
3. **部分单测偏基础设施**：业务模块（gallery/kunlun/gate 大文件如 ark.js 917 行、scene.js 942 行）测试覆盖薄。
4. **类型化程度低**：checkJs 只是半类型安全，长期建议渐进迁移 TS。
5. **token 鉴权为单静态口令 + query 传递**：无账号体系、无 HTTPS 层面的会话管理，query 中传 token 有日志泄露风险，适合私用但不宜多用户公开运营。
6. **沙箱观察到的 I/O 异常不计入代码问题**（为本次环境挂载问题）。

## 三、代码价值估算

| 维度 | 评估 |
|---|---|
| 工程参考价值 | 中高。分层架构、测试、CI、安全加固模式可作为个人/中小项目范本 |
| 产品价值 | 中。面向个人运营的私享画廊，功能独特（3D 展厅+审批门+社区互动），但通用性有限，难以直接商业化 |
| 可复用价值 | 中。scene/kunlun 等强业务耦合复用难；core/（引擎、状态机、空间索引、worker 池）可直接复用到其他 Three.js 项目 |
| 市场对标 | 类似"虚拟展厅 SaaS"（如 Artsteps、VART 虚拟展厅）的轻量自建版 |

**综合估值（外包口径）**：整包重做等价成本约 **25–35 万元人民币**（见工作量），但作为二手资产/代码转让，因业务特殊性与单人维护属性，市场价约 **5–10 万元**；若含完整部署、文档与后续维护承接，可上浮至 10–15 万元。

## 四、工作量估算

- **自研代码量**：约 1.6–1.7 万行（剔除 vendor/ 约 3.5 万行第三方与生成资源）。
- **按功能模块估算**（1 名熟悉 Three.js 的全栈工程师）：

| 模块 | 人日 |
|---|---|
| 引擎基础设施（loop/事件/状态机/空间索引/worker池/资源管理） | 20–25 |
| 3D 场景（画廊、沙海地形、媒体墙、物理、玩家控制、后期特效） | 45–60 |
| 昆仑剧情线（ark/灵蕴/终章等 14 个模块） | 30–40 |
| 门禁/上传/设置等交互系统 | 20–25 |
| 后端（路由/文件/审批/聊天/统计/SSE/TTS/大屏等 17 个 lib 模块） | 25–30 |
| 管理后台 + 各 HTML 页面（15 个页面） | 15–20 |
| 测试、CI、工程化、部署与文档 | 15–20 |
| **合计** | **约 170–220 人日 ≈ 8–11 人月** |

- 若团队 3 人并行并含沟通开销，日历周期约 3.5–5 个月。
- 外包报价参考（一线城市全栈 2000–3000 元/人日）：**约 34–66 万元区间**；考虑实际为一人在 AI 辅助下完成，重做成本取中性估计 **25–35 万元** 更合理。

## 五、改进建议（按优先级）

1. 拆分 server.js 路由为模块化 route 注册器；token 改为 header + 会话过期机制。
2. 收敛 CSP（去 unsafe-eval，nonce 化内联脚本）。
3. 为 ark.js/scene.js 等大文件补充单元测试并进一步拆分职责。
4. 渐进引入 TypeScript（优先 shared/core 层）。
5. 上传链路补充速率限制与配额，防止滥用。

---
*本报告基于对仓库源码、package.json、server.js、既有审计文档与测试文件的实际检查。*
