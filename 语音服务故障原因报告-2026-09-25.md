# 语音服务故障原因报告(2026-09-25)

## 一、故障现象

游戏内角色台词朗读(昆仑灵鉴 TTS)不可用/退化。主人提供新的小米 Token Plan 凭据
(`https://token-plan-sgp.xiaomimimo.com/v1` + tp- 新钥匙)要求排查并接入。

## 二、根因(实测证据)

| 通道 | 端点 | 实测(2026-09-25 服务器侧) | 结论 |
| --- | --- | --- | --- |
| Token Plan CN 订阅(原钥匙) | token-plan-cn.xiaomimimo.com/v1 | **HTTP 401 Invalid API Key** | **根因:订阅钥匙已失效** |
| 按量 sk(MIMO_API_KEY) | api.xiaomimimo.com/v1 | HTTP 200,正常出音频 | 兜底链仍活着 |
| **Token Plan SGP(新钥匙)** | token-plan-sgp.xiaomimimo.com/v1 | **HTTP 200,正常出音频** | 新钥匙有效 |

- 官方文档核实:对 SGP 端点执行 `GET /v1/models`,返回模型清单含
  `mimo-v2.5-tts / mimo-v2.5-tts-voiceclone / mimo-v2.5-tts-voicedesign / mimo-v2.5-asr /
  mimo-v2.5 / mimo-v2.5-pro / mimo-v2.6-flash / mimo-v2.6-pro`——
  **Token Plan 订阅已迁至 SGP 区域,TTS 全家族在列**;原 CN 钥匙随之作废(401)。
- 调用形状不变:仍是 OpenAI 兼容 `POST /v1/chat/completions` + `audio:{format,voice}`,
  现有代码(`lib/aichannels.js synthTts`)零改动兼容。

## 三、影响链分析

`lib/tts.js handleTts → lib/aichannels.js synthTts` 的三级防线:
MiMo TP(订阅) → MiMo sk(按量) → edge-tts(本地)。

- 原钥匙失效后,TP 通道每次都 401 静默失败,由 sk/edge-tts 兜底,
  表现为:角色专属音色失效(按量通道音色覆盖不全)、延迟抖动(edge-tts 实测
  12.6s/318KB,对比 MiMo 云端 1.8s/13KB)、以及 sk 余额耗尽时彻底无服务。
- 设计上失败静默(502 由前端吞掉),故无报错弹窗,只有"语音没声音/音色不对"的体验。

## 四、修复动作(零代码改动)

1. 服务器 `/opt/gallery/.env` 先备份(`.env.bak-<时间戳>`),再追加:
   - `MIMO_TP_BASE_URL=https://token-plan-sgp.xiaomimimo.com/v1`
   - `MIMO_TP_API_KEY=tp-…(新钥匙,只存服务器 .env,不进仓库)`
   - 同文件后值覆盖前值,原 CN 钥匙行自然失效;文本与语音共用 tp 通道,一并恢复。
2. `pm2 restart gallery --update-env`;已确认 pm2 环境无旧钥匙残留
   (config.js 的 dotenv 不覆盖已存在环境变量,pm2 干净时 .env 才是权威)。

## 五、修复后验证(全绿)

| 验证项 | 结果 |
| --- | --- |
| synthTts 直连(服务器,带 .env) | OK 13368B @2.6s(MiMo 云端) |
| 应用内 `GET /api/tts`(pm2 进程,127.0.0.1) | 200,13080B @1.8s |
| **公网 HTTPS `GET /api/tts`**(真实游戏路径) | **200,14376B @4.0s** |
| chatText 文本链(`by=mimo-tp`) | OK @2.2s,回复正常 |
| 官方 /models(SGP) | TTS 全家族在列 |

## 六、回滚方案

`cp /opt/gallery/.env.bak-<时间戳> /opt/gallery/.env && pm2 restart gallery --update-env`
(回滚后 TP 通道恢复 401 状态,语音由 sk/edge-tts 兜底)。

## 七、后续建议

1. `scripts/r2-upload-models.cjs` 走 S3 SDK 需要 R2_ENDPOINT/R2_ACCESS_KEY/R2_SECRET_KEY,
   服务器 .env 里只有 CF_R2_TOKEN(REST 形态)——两套凭据形态不一致,待统一。
2. 合成链对 TP 401 无告警(静默设计),建议在 admin 错误追踪里加一条
   "TTS 主通道连续失败"计数,下次钥匙过期可主动发现而非靠体验退化察觉。
